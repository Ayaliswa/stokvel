import "package:flutter/material.dart";

class StokvelRegFormScreen extends StatelessWidget {
  const StokvelRegFormScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
